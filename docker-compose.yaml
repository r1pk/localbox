name: localbox

services:
    php:
        build:
            context: .
            dockerfile: ./docker/frankenphp/Dockerfile
        ports:
            - "${DOCKER_HOST_PORT:-8000}:80"
        volumes:
            - "./docker/frankenphp/Caddyfile:/etc/frankenphp/Caddyfile:ro"
            - "./docker/frankenphp/conf.d/default.ini:/usr/local/etc/php/conf.d/default.ini:ro"
            - "./docker/frankenphp/conf.d/xdebug.ini:/usr/local/etc/php/conf.d/zz-xdebug.ini:ro"
            - "./storage:/storage"
            - "./:/app"
        extra_hosts:
            - "host.docker.internal:host-gateway"
        tty: true
